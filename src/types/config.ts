type Config = {
    enabled: boolean
    minPrice: number
    maxPrice: number
    minBeds: number
    furnished: boolean
    location?: string
    slackIcon: string
    name: string
}

export default Config
